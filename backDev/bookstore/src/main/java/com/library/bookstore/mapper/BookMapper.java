package com.library.bookstore.mapper;
/*

import com.library.bookstore.dto.BookDto;
import com.library.bookstore.entity.Book;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;

@Getter
@Setter
@NoArgsConstructor
public class BookMapper {

    // convert entity Book to BookDto
    public static BookDto mapToBookDto(Book book) {
        BookDto bookDto = new BookDto();
        bookDto.setBookName(book.getBookName());
        bookDto.setIsbn(book.getIsbn());
        bookDto.setPrice(book.getPrice());
        bookDto.setEditor(book.getEditor());
        bookDto.setType(book.getType());
        bookDto.setDatePublication(book.getDatePublication());
        bookDto.setDescription(book.getDescription());
        return bookDto;
    }

    // convert entity BookDto to Entity Book
    public static Book mapToEntityBook(BookDto bookDto) {
        Book book = new Book();
        book.setBookName(bookDto.getBookName());
        book.setIsbn(bookDto.getIsbn());
        book.setPrice(bookDto.getPrice());
        book.setEditor(bookDto.getEditor());
        book.setType(bookDto.getType());
        book.setDescription(bookDto.getDescription());
        book.setDatePublication(bookDto.getDatePublication());
        return book;
    }
}

 */
